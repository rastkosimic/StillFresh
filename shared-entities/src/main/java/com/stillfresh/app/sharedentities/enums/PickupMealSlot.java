package com.stillfresh.app.sharedentities.enums;

/**
 * Derived (non-persisted) grouping for pickup time-of-day.
 * Used by the UI to display sections like "Collect for lunch" / "Collect for dinner".
 */
public enum PickupMealSlot {
    BREAKFAST,
    LUNCH,
    DINNER,
    OTHER
}


