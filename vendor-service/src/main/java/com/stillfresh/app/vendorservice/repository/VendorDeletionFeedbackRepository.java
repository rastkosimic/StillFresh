package com.stillfresh.app.vendorservice.repository;

import com.stillfresh.app.vendorservice.model.VendorDeletionFeedback;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VendorDeletionFeedbackRepository extends JpaRepository<VendorDeletionFeedback, Long> {
}
